const {ArrayType, FixMeLocation, RedactedFixMe, Enum0} = require("../types.js");

const {FixMe} = require("../models.js");

/**
 * res_output
 * @param rolls: ArrayType(RedactedFixMe)
 */

/**
 * res_input
 */
module.exports = async function process_rollsByMe(res_input, res_output) {
	// TODO: implement
	return res_output;
}