const {ArrayType, FixMeLocation, Enum0} = require("../types.js");

const {FixMe} = require("../models.js");

/**
 * res_output
 * @param location: FixMeLocation
 * @param commitHash: String
 * @param text: String
 */

/**
 * res_input
 */
module.exports = async function process_roll(res_input, res_output) {
	return res_output;
}